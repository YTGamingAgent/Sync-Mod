package net.stacking.sync_mod.networking;

import net.stacking.sync_mod.Sync_mod;

public class SyncPackets {
    public static void registerServer() {
        Sync_mod.LOGGER.info("Registering server packets");
        // Register server-side packet handlers here
    }

    public static void registerClient() {
        Sync_mod.LOGGER.info("Registering client packets");
        // Register client-side packet handlers here
    }
}
